# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/
.env

# Telethon session files — contain auth tokens, never commit
*.session
*.session-journal

# Downloaded audio (temp)
downloads/

# Local env
.env

# SoundCloud token — stored in GitHub Actions Secret, not git
.soundcloud_token.json

# State (committed by CI, ignore local)
# state.json is intentionally tracked by git

# macOS
.DS_Store
